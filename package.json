{
  "name": "factoring",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "dist": "next build && next export -o docs/",
    "deploy": "next build && next export -o docs && git checkout -- docs/.nojekyll && git add docs && git commit -m \"chore: regen site\" && git push origin main",
    "start": "next start"
  },
  "dependencies": {
    "classnames": "^2.2.6",
    "next": "10.0.5",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "sass": "^1.32.2"
  }
}
